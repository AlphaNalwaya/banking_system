%h1
  Bank Name: #{@bank.name}

= button_tag :new_account, id: "btn-new-account", class: "btn btn-primary" do |account|
  New Transaction	
end

= link_to bank_path(@bank.account.id), class: "btn btn-default" do
  Back to #{@bank.account}

= link_to bank_path, class: "btn btn-default" do
	Back to Banks

= render partial: "list", locals: { bank: @bank }

= link_to edit_bank_path(@bank.id), class: "btn btn-info" do
  Edit Bank

= link_to bank_path(@account.id), method: :delete, class: "btn btn-danger", data: { confirm: "Are you sure?" } do
  Delete Bank

#parameters{ data: { bank_id: @bank.id } }

= javascript_include_tag "banks/Show"
:javascript
  $(document).ready(function() {
    Show.init();
  });