%h1
  User: #{@user}
  %small
    #{@user.name}

= link_to edit_user_path(@user), class: "btn btn-info" do
  Edit User    

= link_to new_user_account_path(@user.id), class: "btn btn-primary" do 
  New Account    

= render partial: "accounts", locals: {accounts: @user.accounts}    